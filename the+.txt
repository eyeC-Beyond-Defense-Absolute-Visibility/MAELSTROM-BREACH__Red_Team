Maelstrom-Breach/
├── src/
│   ├── main.cpp                 # The Maelstrom Controller (C2)
│   ├── payloads/                # Attack scripts & binaries
│   │   ├── reverse_shell.py
│   │   ├── ransom_sim.cpp
│   │   └── data_exfil.sh
│   └── modules/
│       ├── Scanner.cpp          # Automated Reconnaissance
│       └── ExploitEngine.cpp    # Payload delivery logic
├── configs/
│   ├── campaign_beta.json       # Scenario: Step-by-step attack sequence
│   └── targets.json             # List of vulnerable nodes (Metasploitable, etc.)
├── build/
├── CMakeLists.txt
└── launch_maelstrom.sh          # Start the "Storm"










🧠 Pourquoi inclure ces vecteurs dans ton projet ?
L'intérêt n'est pas l'attaque elle-même, mais la validation de la défense :

DDoS (Déni de Service) : Cela teste la capacité de Sovereign-Shield (via Cilium/eBPF) à implémenter des limites de débit (Rate Limiting) ou à rejeter des paquets suspects au niveau du Kernel avant qu'ils ne touchent l'application.

SQL Injection (SQLi) : C'est une attaque de couche 7 (Applicative). Cela prouve que ton Sentinel-Trace peut inspecter le contenu des requêtes HTTP (si tu configures l'observabilité L7) et détecter des patterns de caractères malveillants.

Reverse Shell : C'est l'étape finale de compromission système. C'est ici que ton Reasoning Engine (dans Sentinel-Trace) doit faire la corrélation entre "L'application Web a reçu une requête SQLi" et "L'application Web vient de lancer un processus /bin/bash".



🎯 Ce que cela apporte à ton projet :
La détection par Sentinel-Trace : Le script d'exfiltration utilise curl vers une IP externe. Ton moteur de corrélation va voir un processus suspect (tar) suivi d'une connexion réseau sortante.

La réponse de Sovereign-Shield : Dès que le Ransomware commence à chiffrer (plusieurs écritures fichiers rapides), Sentinel-Trace donne l'ordre à Sovereign-Shield de tuer le processus et d'isoler le nœud pour stopper la propagation.

C'est maintenant une "Full-Chain" attaque/défense. Souhaites-tu que nous passions à la création de ta Landing Page GitHub pour présenter cette trilogie eyeC de manière magistrale ?